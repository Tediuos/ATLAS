# Core HTTP + HTML parsing
openai>=1.40.0
langchain>=1.0,<2
langchain-openai>=1.0,<2
langgraph>=1.0,<2
langgraph-checkpoint-sqlite>=3.0,<4
httpx>=0.27.0
selectolax>=0.3.21
defusedxml>=0.7,<1
nh3>=0.3,<1

# Environment / config
python-dotenv>=1.0.0
pydantic>=2.7.0

# Database
sqlalchemy>=2.0.36

# Templating (SEO report)
jinja2>=3.1.4

# Streamlit UI
streamlit>=1.50,<2
pandas>=2.2.0

# PDF export (optional — requires system libs, see docs)
# weasyprint>=62.0

# Development / testing
pytest>=8.0.0
